import React from 'react';

                //Destructuring
const Location = ({ city }) => {
   // Normal   
  //  const city = props.city

    return (
        <div><h1>{city}</h1></div>)
};

export default Location;